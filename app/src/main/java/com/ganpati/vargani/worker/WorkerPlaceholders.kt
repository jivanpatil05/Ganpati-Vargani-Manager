package com.ganpati.vargani.worker

/**
 * Placeholder for future WorkManager jobs (e.g. scheduled local backup reminders
 * or eventual cloud sync). Kept empty so the package matches the architecture layout
 * without introducing unused workers in the offline-first MVP.
 */
object WorkerPlaceholders
